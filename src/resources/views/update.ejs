<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    />
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  </head>
  <style>
    html,
    body {
      margin: 0;
      width: 100%;
      height: 100%;
      box-sizing: border-box;
      background: rgb(207, 207, 207);
    }

    .main {
      height: 100%;
      width: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .box-form {
      background-color: rgb(0, 105, 9);
      box-shadow: 10px 10px 20px rgba(0, 0, 0, 0.6);
      padding: 1rem 2rem;
      border-radius: 6px;
    }
    h2 {
      color: white;
      font-weight: 700;
    }
    label {
      font-size: 18px;
      font-weight: 600;
      color: white;
    }
  </style>

  <body>
    <div class="container main">
      <div class="col-3"></div>
      <div class="col-6 box-form">
        <a href="/admin/result" class="btn btn-primary">BACK</a>
        <form action="/admin/update/<%= result._id %>" method="POST">
          <h2 class="text-center">Update</h2>
          <!-- <div class="form-group">
                    <label>Mã kết quả: </label>
                    <input class="form-control" type="text" name="id" placeholder="Mã lớp">
                </div> -->
          <div class="form-group">
            <label>Mã SV: </label>
            <input
              class="form-control"
              type="text"
              value="<%= result.maSV %>"
              name="maSV"
            />
          </div>
          <div class="form-group">
            <label>Mã MH: </label>
            <input
              class="form-control"
              type="text"
              value="<%= result.maMH %>"
              name="maMH"
            />
          </div>
          <div class="form-group">
            <label>Điểm Thi: </label>
            <input
              class="form-control"
              type="number"
              value="<%= result.diemThi %>"
              name="diemThi"
            />
          </div>
          <div class="button">
            <input
              id="add-btn"
              class="btn btn-primary"
              type="submit"
              value="Update"
            />
          </div>
        </form>
      </div>
      <div class="col-3"></div>
    </div>
  </body>
</html>
